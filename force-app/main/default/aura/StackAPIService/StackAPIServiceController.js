({
	searchStackExchange : function(component, event, helper) {
		helper.search(component, event);
	},

	getTagInfo : function(component, event, helper){
		helper.getTagDetails(component, event);
	}
})